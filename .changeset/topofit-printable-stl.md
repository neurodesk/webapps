---
"topofit": patch
"@neurodesk/topofit": patch
---

Export cortical surfaces as printable STL from TopoFit: a dialog sets the triangle fraction to keep (10 to 100%, default 25) and Humphrey smoothing iterations (0 to 20, default 0), niimath simplifies and smooths each surface, and the app writes a binary STL per surface.
