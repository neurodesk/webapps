---
"topofit": minor
"@neurodesk/topofit": minor
---

Preserve the selected viewer layout when viewing or toggling surfaces. Selecting a surface in ACS now shows its boundaries on the slices instead of switching to 3D.

Record surface analysis measurements and the processing manifest in the technical log instead of offering them as downloadable results.

Honor explicit anatomy visibility changes in FreeBrowse and retain that choice when selecting another surface.

Add optional outward mid-surface normal arrows with physical spacing and length controls, following visible surfaces and using the same area-weighted normals as the CSV export.
