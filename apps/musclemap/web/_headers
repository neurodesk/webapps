# Cloudflare Pages headers. Production COOP/COEP so SharedArrayBuffer / multi-threaded
# ONNX Runtime works (the coi-serviceworker.js remains as a fallback for other hosts).
/*
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Cross-Origin-Resource-Policy: same-origin
